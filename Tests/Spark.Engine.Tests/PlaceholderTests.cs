using Xunit;

namespace Spark.Engine.Tests;

/// <summary>
/// Placeholder test to verify the test infrastructure is correctly configured.
/// Replace with real unit tests as the engine's testable surface area grows.
/// </summary>
public class PlaceholderTests
{
    [Fact]
    public void TestInfrastructure_IsConfiguredCorrectly()
    {
        // This test exists solely to confirm that:
        // 1. The xUnit runner discovers and executes tests in this project.
        // 2. The project references resolve correctly.
        // 3. `dotnet test` succeeds from the solution root.
        Assert.True(true);
    }
}
